# Инструкция по установке и запуску

## Шаг 1: Установка локального сервера

Выберите один из вариантов:

### Вариант А: OpenServer (рекомендуется для Windows)
1. Скачайте OpenServer с официального сайта: https://ospanel.io/
2. Установите и запустите OpenServer
3. Дождитесь зеленого флажка в трее

### Вариант Б: XAMPP
1. Скачайте XAMPP: https://www.apachefriends.org/ru/index.html
2. Установите XAMPP
3. Запустите Apache и MySQL через панель управления

### Вариант В: Встроенный PHP-сервер (для разработки)
1. Установите PHP: https://www.php.net/downloads
2. Установите MySQL: https://dev.mysql.com/downloads/installer/

## Шаг 2: Размещение файлов проекта

### Для OpenServer:
1. Откройте папку OpenServer
2. Перейдите в `domains`
3. Скопируйте папку `KT2-lab1` в `domains`
4. Перезапустите OpenServer

### Для XAMPP:
1. Откройте папку XAMPP
2. Перейдите в `htdocs`
3. Скопируйте папку `KT2-lab1` в `htdocs`

### Для встроенного сервера:
Просто запомните путь к папке `KT2-lab1`

## Шаг 3: Создание базы данных

### Способ 1: Через phpMyAdmin (OpenServer/XAMPP)
1. Откройте браузер
2. Перейдите по адресу: http://localhost/phpmyadmin
3. Нажмите "Создать базу данных" или "SQL"
4. Откройте файл `database.sql` из папки проекта
5. Скопируйте весь код и вставьте в окно SQL
6. Нажмите "Выполнить" или "Go"

### Способ 2: Через командную строку
```bash
mysql -u root -p < database.sql
```
(Если попросит пароль, по умолчанию он пустой - просто нажмите Enter)

## Шаг 4: Настройка подключения к БД

1. Откройте файл `config.php`
2. Проверьте настройки:
   ```php
   define('DB_HOST', 'localhost');     // Обычно не меняется
   define('DB_NAME', 'medcentr_db');   // Имя базы данных
   define('DB_USER', 'root');          // Пользователь (обычно root)
   define('DB_PASS', '');              // Пароль (обычно пустой)
   ```
3. Если у вас другие настройки MySQL, измените их

## Шаг 5: Запуск проекта

### Для OpenServer:
1. Откройте браузер
2. Перейдите: http://localhost/KT2-lab1
   или http://kt2-lab1 (если настроили домен)

### Для XAMPP:
1. Откройте браузер
2. Перейдите: http://localhost/KT2-lab1

### Для встроенного сервера:
1. Откройте командную строку (cmd)
2. Перейдите в папку проекта:
   ```bash
   cd путь\к\KT2-lab1
   ```
3. Запустите сервер:
   ```bash
   php -S localhost:8000
   ```
4. Откройте браузер: http://localhost:8000

## Проверка работы

1. **Главная страница** (index.php):
   - Должна отображаться текущая дата и день недели
   - Новости загружаются из базы данных
   - Специалисты загружаются из базы данных

2. **Страница записи** (appointment.php):
   - Форма записи к врачу
   - Список врачей загружается из БД
   - После отправки данные сохраняются в БД

3. **Просмотр записей** (view_appointments.php):
   - Таблица со всеми записями
   - Можно посмотреть все заявки

## Возможные проблемы и решения

### Ошибка подключения к БД
**Проблема:** "Ошибка подключения к базе данных"
**Решение:**
- Проверьте, запущен ли MySQL
- Проверьте настройки в `config.php`
- Убедитесь, что база данных создана

### Не отображаются изображения
**Проблема:** Вместо фото пустые блоки
**Решение:**
- Убедитесь, что папка `image` скопирована
- Проверьте пути к изображениям в базе данных

### Ошибка 404
**Проблема:** Страница не найдена
**Решение:**
- Проверьте правильность URL
- Убедитесь, что сервер запущен
- Проверьте, что файлы в правильной папке

### Не работает форма
**Проблема:** После отправки формы ничего не происходит
**Решение:**
- Проверьте, что таблица `appointments` создана
- Посмотрите логи ошибок PHP
- Убедитесь, что все поля заполнены

## Тестовые данные

После импорта `database.sql` в базе будут:

**Новости:** 4 записи
- Скидка на диагностику
- Новое оборудование МРТ
- Программа вакцинации
- Открытие детского отделения

**Специалисты:** 6 врачей
- Иванова М.П. - Терапевт
- Смирнов А.В. - Кардиолог
- Петрова М.С. - Педиатр
- Волков Д.В. - Невролог
- Иванова А.И. - УЗИ
- Волков И.В. - Эндокринолог

## Контакты для вопросов

Если возникли проблемы:
1. Проверьте все шаги установки
2. Посмотрите логи ошибок PHP
3. Убедитесь, что версия PHP >= 7.4
4. Проверьте, что MySQL запущен

Удачи с лабораторной работой! 🚀
