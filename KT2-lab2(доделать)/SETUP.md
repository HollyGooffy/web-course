# 🚀 Инструкция по запуску

## 1️⃣ Настройка MySQL

### Создайте базу данных:
```sql
CREATE DATABASE avito_parser CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### Или через командную строку:
```bash
mysql -u root -p
```
Затем выполните:
```sql
CREATE DATABASE avito_parser;
EXIT;
```

## 2️⃣ Настройка Backend

1. Перейдите в папку backend:
```bash
cd backend
```

2. Скопируйте файл `.env.example` в `.env`:
```bash
copy .env.example .env
```

3. Отредактируйте `backend/.env` и укажите ваши данные MySQL:
```env
PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=ваш_пароль
DB_NAME=avito_parser
DB_PORT=3306
```

## 3️⃣ Запуск проекта

### Вариант 1: Запустить все сразу (из корневой папки)
```bash
npm run dev
```

### Вариант 2: Запустить отдельно

**Backend** (терминал 1):
```bash
cd backend
npm run dev
```

**Frontend** (терминал 2):
```bash
cd frontend
npm run dev
```

## 4️⃣ Открыть приложение

Откройте браузер: http://localhost:3000

## ✅ Проверка

1. Backend должен запуститься на порту 5000
2. Frontend должен запуститься на порту 3000
3. В консоли backend должно появиться:
   - ✅ Database initialized successfully
   - 🚀 Server running on http://localhost:5000

## ⚠️ Возможные проблемы

### Ошибка подключения к MySQL:
- Проверьте, что MySQL запущен
- Проверьте правильность данных в `.env`
- Убедитесь, что база данных создана

### Порт уже занят:
- Измените PORT в `backend/.env`
- Или остановите процесс на порту 5000/3000

### Ошибка "Cannot find module":
```bash
npm run install-all
```

## 🎯 Готово!

Теперь можете:
1. Перейти на вкладку "Парсинг"
2. Ввести запрос (например: "Toyota Camry")
3. Выбрать город
4. Нажать "Начать парсинг"
5. Посмотреть результаты на вкладке "Результаты"
