# 🚗 Avito Parser

Веб-приложение для парсинга объявлений об автомобилях с Avito.ru

## 🛠 Технологии

- **Backend**: Node.js, Express.js
- **Frontend**: React, Vite
- **Database**: MySQL
- **Парсинг**: Axios, Cheerio

## 📋 Требования

- Node.js 18+
- MySQL 8.0+
- npm или yarn

## 🚀 Установка

### 1. Клонируйте репозиторий
```bash
git clone <repository-url>
cd avito-parser
```

### 2. Установите зависимости
```bash
npm run install-all
```

### 3. Настройте базу данных

Создайте базу данных MySQL:
```sql
CREATE DATABASE avito_parser CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 4. Настройте переменные окружения

Скопируйте `.env.example` в `.env` в папке `backend`:
```bash
cd backend
copy .env.example .env
```

Отредактируйте `backend/.env`:
```env
PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=avito_parser
DB_PORT=3306
```

## 🎯 Запуск

### Запуск всего проекта (рекомендуется)
```bash
npm run dev
```

### Или запустите отдельно:

**Backend** (порт 5000):
```bash
npm run server
```

**Frontend** (порт 3000):
```bash
npm run client
```

## 📱 Использование

1. Откройте http://localhost:3000
2. Перейдите на вкладку "Парсинг"
3. Введите поисковый запрос (например: "Toyota Camry 2020")
4. Выберите город
5. Укажите количество страниц (1-5)
6. Нажмите "Начать парсинг"
7. Просмотрите результаты на вкладке "Результаты"
8. Изучите статистику на вкладке "Статистика"

## 🔍 Функции

- ✅ Парсинг объявлений с Avito
- ✅ Сохранение в MySQL
- ✅ Поиск и фильтрация
- ✅ Статистика (средняя цена, мин/макс)
- ✅ Удаление объявлений
- ✅ Адаптивный дизайн

## ⚠️ Важно

- Используйте парсинг ответственно
- Не перегружайте сервер Avito (рекомендуется 1-2 страницы)
- Задержка между запросами: 2-4 секунды
- Avito может блокировать частые запросы

## 📂 Структура проекта

```
avito-parser/
├── backend/
│   ├── src/
│   │   ├── config/         # Конфигурация БД
│   │   ├── controllers/    # Контроллеры
│   │   ├── routes/         # Маршруты API
│   │   ├── services/       # Сервис парсинга
│   │   └── server.js       # Точка входа
│   └── package.json
├── frontend/
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   ├── services/       # API клиент
│   │   ├── styles/         # CSS стили
│   │   ├── App.jsx
│   │   └── main.jsx
│   └── package.json
└── package.json
```

## 🔧 API Endpoints

- `POST /api/parse` - Запустить парсинг
- `GET /api/cars` - Получить все объявления
- `GET /api/cars/search` - Поиск с фильтрами
- `GET /api/cars/statistics` - Статистика
- `DELETE /api/cars/:id` - Удалить объявление
- `DELETE /api/cars` - Удалить все

## 📝 Лицензия

MIT
