# 🎵 WelcomeHome Festival Platform

Современная платформа для управления музыкальными фестивалями с административной панелью и публичным интерфейсом. Построена на React 19, TypeScript и Feature-Sliced Design архитектуре.

## ✨ Основные возможности

### 🎪 Для посетителей
- **Афиша событий** - просмотр предстоящих фестивалей и концертов
- **Коллекционные карточки** - просмотр и покупка карточек участников
- **Мерч-магазин** - каталог официальной продукции
- **Подача заявок** - регистрация групп для участия в фестивалях
- **Информация о группах** - каталог участвующих музыкальных коллективов

### 🛠️ Для администраторов
- **Управление фестивалями** - создание, редактирование, планирование событий
- **Управление группами** - модерация участников, управление составами
- **Система заявок** - обработка заявок от групп, статистика
- **Управление мерчем** - добавление товаров, управление складом
- **Карточки участников** - создание и управление коллекционными карточками
- **Аналитика** - детальная статистика по всем разделам
- **Управление контентом** - загрузка изображений, описаний

## 🚀 Быстрый старт

### Требования
- Node.js 18+
- pnpm (рекомендуется) или npm

### Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd frontend-react.js

# Установка зависимостей
pnpm install

# Настройка переменных окружения
cp .env.example .env
```

### Конфигурация

Создайте файл `.env` с настройками:

```env
# API Configuration
VITE_API_URL=http://localhost:3001/api

# Optional: Development settings
VITE_DEV_MODE=true
```

### Запуск

```bash
# Режим разработки
pnpm dev

# Сборка для продакшна
pnpm build

# Предварительный просмотр сборки
pnpm preview

# Линтинг кода
pnpm lint
```

## 🏗️ Архитектура

Проект построен по методологии **Feature-Sliced Design (FSD)** для максимальной масштабируемости и поддерживаемости.

```
src/
├── app/                    # Конфигурация приложения
│   ├── App.tsx            # Корневой компонент
│   └── main.tsx           # Точка входа
│
├── entities/              # Бизнес-сущности
│   ├── application/       # Заявки групп
│   ├── event/            # События и концерты
│   ├── festival/         # Фестивали
│   ├── group/            # Музыкальные группы
│   └── merch/            # Товары
│
├── features/             # Бизнес-функции
│   ├── auth/             # Аутентификация
│   ├── card-management/  # Управление карточками
│   ├── event-management/ # Управление событиями
│   ├── festival-management/ # Управление фестивалями
│   ├── group-management/ # Управление группами
│   ├── merch-management/ # Управление товарами
│   └── participant-cards/ # Карточки участников
│
├── pages/                # Страницы приложения
│   ├── admin/            # Административные страницы
│   │   ├── dashboard/    # Главная панель
│   │   ├── application/  # Заявки
│   │   ├── groups/       # Группы
│   │   ├── poster/       # Афиша
│   │   ├── merch/        # Товары
│   │   ├── cards/        # Карточки
│   │   └── stats/        # Статистика
│   ├── home/             # Публичные страницы
│   └── admin-login/      # Вход в админку
│
├── shared/               # Переиспользуемый код
│   ├── api/              # API клиент и эндпоинты
│   ├── ui/               # UI компоненты
│   ├── hooks/            # Общие хуки
│   ├── lib/              # Утилиты
│   └── constants/        # Константы
│
└── widgets/              # Композитные блоки
    ├── admin-sidebar/    # Боковая панель админки
    ├── cards-management/ # Виджет управления карточками
    ├── header/           # Шапка сайта
    └── footer/           # Подвал сайта
```

### Принципы архитектуры

- **Модульность** - каждая фича изолирована и самодостаточна
- **Переиспользование** - общие компоненты в shared слое
- **Типизация** - строгая типизация с TypeScript
- **Консистентность** - единые паттерны во всем проекте

## 🔧 Технологический стек

### Основные технологии
- **React 19** - UI библиотека с новейшими возможностями
- **TypeScript** - статическая типизация
- **Vite** - быстрая сборка и разработка
- **React Router** - маршрутизация

### UI и стилизация
- **CSS Modules** - изолированные стили
- **Lucide React** - иконки
- **Framer Motion** - анимации
- **Responsive Design** - адаптивная верстка

### Работа с данными
- **Axios** - HTTP клиент
- **React Query** - кеширование и синхронизация данных
- **React Hook Form** - управление формами
- **Zod** - валидация схем

### Качество кода
- **ESLint** - линтинг кода
- **TypeScript ESLint** - правила для TypeScript
- **Prettier** - форматирование (рекомендуется)

## 🎨 Особенности UI/UX

### Дизайн-система
- **Консистентная цветовая палитра** - музыкальная тематика
- **Адаптивная верстка** - работает на всех устройствах
- **Анимации** - плавные переходы и микроинтерракции
- **Доступность** - соответствие стандартам a11y

### Ключевые компоненты
- **Модальные окна** - для создания и редактирования
- **Карточки** - для отображения контента
- **Формы** - с валидацией и обработкой ошибок
- **Таблицы** - для административных данных
- **Загрузка изображений** - с предпросмотром и сжатием

## 🔐 Аутентификация и безопасность

### JWT Authentication
- **Токены доступа** - для API запросов
- **Автоматическое обновление** - refresh token механизм
- **Защищенные маршруты** - AuthGuard компоненты
- **Роли пользователей** - разграничение доступа

### Безопасность
- **Валидация данных** - на клиенте и сервере
- **Санитизация** - очистка пользовательского ввода
- **HTTPS** - безопасная передача данных
- **CSP заголовки** - защита от XSS

## 📱 Адаптивность

Приложение полностью адаптировано для:
- **Desktop** (1440px+) - полный функционал
- **Tablet** (768px-1439px) - адаптированный интерфейс
- **Mobile** (320px-767px) - мобильная версия

## 🧪 Тестирование

```bash
# Запуск тестов (когда будут добавлены)
pnpm test

# Покрытие кода
pnpm test:coverage

# E2E тесты
pnpm test:e2e
```

## 📦 Сборка и деплой

### Production сборка
```bash
pnpm build
```

### Анализ бандла
```bash
pnpm build --analyze
```

### Docker (опционально)
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN pnpm install
COPY . .
RUN pnpm build
EXPOSE 3000
CMD ["pnpm", "preview"]
```

## 🤝 Разработка

### Структура коммитов
```
feat: добавить новую функцию
fix: исправить баг
refactor: рефакторинг кода
style: изменения стилей
docs: обновление документации
test: добавление тестов
```

### Правила кода
- Используйте TypeScript для всех компонентов
- Следуйте FSD архитектуре
- Пишите самодокументируемый код
- Добавляйте JSDoc для сложных функций

### Добавление новой фичи

1. Создайте папку в `src/features/`
2. Добавьте структуру: `ui/`, `hooks/`, `lib/`, `model/`
3. Экспортируйте через `index.ts`
4. Добавьте в соответствующую страницу

## 📄 Лицензия

Этот проект является частью WelcomeHome Festival Platform.

## 👥 Команда

Разработано с ❤️ для музыкального сообщества.

---

**Версия:** 1.0.0  
**Последнее обновление:** Январь 2025
