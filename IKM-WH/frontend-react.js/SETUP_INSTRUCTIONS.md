# Инструкции по настройке и запуску

## Быстрый старт

### 1. Установка зависимостей
```bash
npm install
# или
pnpm install
```

### 2. Настройка переменных окружения
Скопируйте файл `.env.example` в `.env`:
```bash
cp .env.example .env
```

Убедитесь, что в `.env` указан правильный URL бэкенда:
```env
VITE_API_BASE_URL=http://localhost:3001
```

### 3. Запуск бэкенда
Перейдите в папку бэкенда и запустите сервер:
```bash
cd ../backend-node.js
npm run dev
```

Бэкенд должен быть доступен по адресу `http://localhost:3001`

### 4. Запуск фронтенда
В папке фронтенда выполните:
```bash
npm run dev
```

Фронтенд будет доступен по адресу `http://localhost:5173`

## Архитектура доступа

### 🌐 Публичные страницы (без авторизации)
- **Главная страница** (`/`) - Обзор фестиваля и последние события
- **События** (`/events`) - Полный список всех событий
- **Группы** (`/groups`) - Информация о группах
- **Мерч** (`/merch`) - Каталог товаров
- **Заявки** (`/applications`) - Подача заявок

### 🔒 Защищенные страницы (только для авторизованных)
- **Панель управления** (`/dashboard`) - Статистика и управление
- Все административные функции

### 🔑 Авторизация
- **Вход** (`/login`) - Простая форма входа
- **Регистрация** (`/register`) - Создание нового аккаунта

## Структура проекта

```
src/
├── app/                    # Основное приложение
│   ├── App.tsx            # Главный компонент с маршрутизацией
│   └── main.tsx           # Точка входа
├── features/              # Функциональные модули
│   └── auth/              # Модуль авторизации (простые формы)
├── pages/                 # Страницы приложения
│   ├── HomePage.tsx       # Главная (публичная)
│   ├── EventsPage.tsx     # События (публичная)
│   ├── LoginPage.tsx      # Вход (простой стиль)
│   ├── RegisterPage.tsx   # Регистрация (простой стиль)
│   └── DashboardPage.tsx  # Панель управления (защищенная)
├── shared/                # Общие ресурсы
│   ├── api/              # API клиенты и конфигурация
│   ├── components/       # Переиспользуемые компоненты
│   ├── contexts/         # React контексты
│   ├── hooks/            # Кастомные хуки
│   ├── providers/        # Провайдеры
│   └── types/            # TypeScript типы
└── widgets/              # Виджеты (сложные компоненты)
```

## Основные возможности

### ✅ Публичный доступ
- Просмотр всех событий без авторизации
- Доступ к информации о группах и мерче
- Подача заявок (если разрешено бэкендом)
- Навигация между разделами

### ✅ Авторизация (простые формы)
- Регистрация с базовой валидацией
- Вход в систему
- Автоматическое сохранение сессии
- Простой, понятный интерфейс

### ✅ Защищенная панель управления
- Статистика (только для авторизованных)
- Управление контентом
- Административные функции

### ✅ API Integration
- Публичные endpoints работают без токена
- Защищенные endpoints требуют авторизации
- Умная обработка ошибок 401

## Доступные страницы

### Публичные (доступны всем)
- `/` - Главная страница фестиваля
- `/events` - Список всех событий
- `/groups` - Информация о группах
- `/merch` - Каталог товаров
- `/applications` - Подача заявок

### Авторизация
- `/login` - Простая форма входа
- `/register` - Простая форма регистрации

### Защищенные (только для авторизованных)
- `/dashboard` - Панель управления со статистикой

## Использование

### Публичный доступ
Пользователи могут свободно просматривать:
- Все события фестиваля
- Информацию о группах
- Каталог мерча
- Подавать заявки (если настроено в бэкенде)

### Авторизация
```tsx
// Простые формы без сложной валидации
// Базовая проверка на клиенте
// Обработка ошибок от сервера
```

### Административный доступ
После авторизации доступна панель управления с:
- Статистикой по событиям, заявкам, пользователям
- Списком последних событий
- Кнопкой выхода

## Стилизация

Используется **простой inline-стиль** вместо Tailwind CSS:
- Понятные, читаемые стили
- Нет зависимости от CSS фреймворков
- Легко кастомизировать
- Быстрая загрузка

## Разработка

### Добавление новых публичных страниц
1. Создайте компонент в `src/pages/`
2. Добавьте маршрут в `src/app/App.tsx` (без `ProtectedRoute`)
3. Добавьте навигацию в header компонентов

### Добавление защищенных страниц
1. Создайте компонент в `src/pages/`
2. Оберните в `ProtectedRoute` в `src/app/App.tsx`

### Изменение стилей форм
Все стили находятся inline в компонентах:
- `LoginForm.tsx` - стили формы входа
- `RegisterForm.tsx` - стили формы регистрации
- Легко изменить цвета, размеры, отступы

## Troubleshooting

### Публичные страницы не загружаются
Проверьте, что бэкенд разрешает публичный доступ к endpoints

### Ошибки авторизации
- Проверьте правильность email/пароля
- Убедитесь, что бэкенд запущен
- Проверьте консоль браузера на ошибки

### Статистика не загружается
Убедитесь, что пользователь авторизован и имеет права доступа

## Дополнительные команды

```bash
# Сборка для продакшена
npm run build

# Предварительный просмотр сборки
npm run preview

# Линтинг
npm run lint
```

Теперь ваш фронтенд работает в публичном режиме с простыми формами авторизации!